<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required so the SDK can scan/switch Wi-Fi and send the EZ (SmartConfig) multicast frames -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Required to read the connected Wi-Fi SSID at runtime (Android 8.1+) during device pairing -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".SmartApp"
        tools:replace="android:allowBackup,android:supportsRtl"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDevice">

        <!-- Tuya/Thing SDK credentials. Some SDK components (e.g. the security prompt)
             read these from manifest meta-data, so they must be present here in addition
             to the ThingHomeSdk.init(appKey, appSecret) call. -->
        <meta-data
            android:name="THING_SMART_APPKEY"
            android:value="***REMOVED***" />
        <meta-data
            android:name="THING_SMART_SECRET"
            android:value="***REMOVED***" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartDevice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>